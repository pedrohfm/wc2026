# Runtime dependencies for the forecasting pipeline (scripts/ + src/wc2026).
# Used by the scheduled GitHub Action; install locally with:
#   pip install -r requirements.txt
pandas
numpy
scipy
openpyxl
